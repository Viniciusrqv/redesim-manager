# =====================================================
# REDESIM MANAGER - Dependências (compatível com Python 3.13)
# =====================================================
# Instale tudo com: pip install -r requirements.txt

# Interface visual
streamlit>=1.39.0
streamlit-extras>=0.4.7
altair>=5.0.0

# Manipulação de dados (versões com wheels prontos para Py 3.13)
pandas>=2.2.3
numpy>=2.1.0
openpyxl>=3.1.5

# --- Banco de dados (produção) ---
# psycopg2-binary: driver Postgres pro Supabase.
# Quando DATABASE_URL não está setada, o app cai pra SQLite local.
psycopg2-binary>=2.9.10
# Cliente Supabase (Auth + Storage)
supabase>=2.9.0
# Cookies persistentes pra "mantenha me conectado" (refresh_token Supabase)
streamlit-cookies-controller>=0.0.4

# Extração de PDF (Cartão CNPJ, AVCB, alvarás)
pdfplumber>=0.11.4
PyMuPDF>=1.24.10
pypdfium2>=4.30.0
pytesseract>=0.3.13       # OCR para Cartões CNPJ com fonte embarcada
Pillow>=10.3.0

# Notificações - Telegram
python-telegram-bot>=21.6
requests>=2.32.3

# Notificações - SMS (opcional)
twilio>=9.3.0

# Variáveis de ambiente
python-dotenv>=1.0.1

# Agendador
schedule>=1.2.2
APScheduler>=3.10.4

# Utilitários
python-dateutil>=2.9.0
unidecode>=1.3.8

# Automação de navegador (consulta Viabilidade no Facilita-SP)
# Após instalar, rode UMA VEZ:  playwright install chromium
playwright>=1.40.0
