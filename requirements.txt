# =====================================================
# REDESIM MANAGER - Dependências (Python 3.13)
# =====================================================
# Otimizado pra arrancada rápida no Streamlit Cloud — só o essencial.

# Interface visual
streamlit==1.58.0  # fixado: 1.58 ainda aceita use_container_width / components.v1.html (evita quebra em update)
streamlit-extras>=0.4.7
altair>=5.0.0

# Manipulação de dados
pandas>=2.2.3
numpy>=2.1.0
openpyxl>=3.1.5

# Banco de dados (produção)
psycopg2-binary>=2.9.10
# Cliente Supabase (Auth)
supabase>=2.9.0
# Cookie persistente do refresh_token Supabase
streamlit-cookies-controller>=0.0.4
# Geração de Word (distratos, ANEXO V)
python-docx>=1.1.2

# Leitura de PDF (lazy-imported em utils/cnae_tools.py — só carrega
# quando você lê um PDF de cartão CNPJ / AVCB / alvará)
pdfplumber>=0.11.4
PyMuPDF>=1.24.10
pypdfium2>=4.30.0
Pillow>=10.3.0
pytesseract>=0.3.10  # OCR p/ cartao CNPJ impresso como PDF (fonte sem Unicode) - requer tesseract-ocr no servidor (ver packages.txt)

# HTTP (Telegram via API direta + BrasilAPI + ReceitaWS + GESTTA)
requests>=2.32.3

# Variáveis de ambiente
python-dotenv>=1.0.1

# Utilitários
python-dateutil>=2.9.0
unidecode>=1.3.8

# =====================================================
# REMOVIDOS do Cloud (não eram usados):
# - python-telegram-bot (usamos só requests pra Telegram)
# - twilio (SMS não usado)
# - schedule + APScheduler (cron via GitHub Actions)
# - playwright (não automatizamos navegação no Cloud)
# - pytesseract (OCR — pesado, casos raros)
#
# Se precisar localmente: pip install <nome>
# =====================================================
